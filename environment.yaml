initialize:
  - run: corepack enable && pnpm install
knowledge:
  - name: lint
    contents: pnpm typecheck  # tsc --noEmit
  - name: test
    contents: pnpm test  # vitest run
  - name: build
    contents: pnpm build
  - name: startup
    contents: pnpm dev  # Next.js dev server
